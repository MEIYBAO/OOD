# 作业一

![alt text](image-11.png)
![alt text](image-12.png)

# 作业二

![alt text](image-13.png)

## unsigned不能修饰浮点数类型
例如：float 和 double。
原因：浮点数本身是用符号位和指数位表示的，无法通过 unsigned 来去掉符号位。即使尝试使用，编译器也会报错。

# 作业三
![alt text](image-14.png)

![alt text](image-15.png)
## 重载函数不能只根据返回值不同区分
![alt text](image-16.png)

## 传参问题

```c++
#include <iostream>
using namespace std;

void fun(int *x,int *y)
{
    int * z = x;
    x = y;
    y = z;
    cout<<*x<<" "<<*y<<endl;
}

int main() {
    int x = 5,y = 10;
    fun(&x,&y);
    cout<<x<<" "<<y<<endl;
    //输出结果为“5 10”
    return 0;
}
```

# 作业四
![alt text](image-17.png)

# 先处理初始化列表再执行构造函数的函数体

构造函数的执行分为两个阶段：
* 初始化阶段：处理初始化列表，初始化所有成员对象（包括显式在列表中的和编译器自动初始化的）。
* 赋值阶段：执行构造函数花括号{}内的函数体。

![alt text](image-18.png)

# 默认访问权限
* struct是public
* class是private

# 作业五

## 默认存储类型是auto

* auto：默认局部变量，生命周期短，存储在栈中。

* register：建议存储在寄存器中，访问速度快，但受限于硬件资源。

* static：延长变量生命周期，局部变量值保留，全局变量限制作用域。

* extern：用于跨文件共享变量或函数。
![alt text](image-20.png)

![alt text](image-19.png)

## 友元
![alt text](image-21.png)
A选项要借助类对象，因为友元函数并不是成员函数

![alt text](image-22.png)

#作业六

![alt text](image-23.png)

![alt text](image-24.png)

![alt text](image-25.png)

![alt text](image-26.png)

![alt text](image-27.png)

![alt text](image-28.png)

![alt text](image-29.png)

![alt text](image-30.png)

![alt text](image-31.png)

# 作业七

![alt text](image-32.png)

题目说的是派生类对象


## 构造的时候先构造基类，再构造组合类
![alt text](image-33.png)

![alt text](image-34.png)

## 虚基类目的

![alt text](image-35.png)

# 作业八

![alt text](image-36.png)
* 运行时多态：虚函数
* 编译时多态：函数重载

![alt text](image-37.png)

* 动态联编：虚函数
* 静态联编：没有virtual，派生类不复写基类的同名函数
* 
![alt text](image-38.png)

![alt text](image-39.png)

![alt text](image-40.png)

![alt text](image-41.png)

![alt text](image-42.png)

![alt text](image-43.png)

![alt text](image-44.png)

虚函数必须有定义且基类可实例化，纯虚函数无定义且基类不可实例化

![alt text](image-45.png)

## 运算符重载

```c++
/*
给定的程序通过重载运算符"=="，判断两个复数是否相等（若相等则返回true，否则返回false）；重载前置"++"运算，使虚部和实部分别加1。
请将程序补充完整，使得程序运行时的输出结果为：
s1: 10 20
s2: 0 0
s1!=s2
s1: 11 21
s2: 11 21
s1=s2

注意：仅允许在指定的下划线处填写内容，并删除下划线编号，不允许增加或删减语句，也不得改动程序中的其他任何内容。
试题源程序：
*/
#include<iostream>
using namespace std;
class comp {
		float real,ima; // 复数实部和虚部
	public:
		comp(float r=0.0,float i=0.0) { real=r; ima=i; }
		comp& operator ++();

/**********FILL**********/
		    bool operator ==  (const comp &a) const {
			if(this->real==a.real && this->ima==a.ima) return true;
			return false;
		}

/**********FILL**********/
		    void show()     { cout<<real<<' '<<ima<<endl; }
};

/**********FILL**********/
    	comp& comp::operator ++ ()      {
	this->real++;
	this->ima++;

/**********FILL**********/
	return      *this     ;
}
int main( ) {
	comp s1(10,20),s2;
	cout<<"s1: "; s1.show();
	cout<<"s2: "; s2.show();
	if(s1==s2) cout<<"s1=s2"<<endl;
	else cout<<"s1!=s2"<<endl;
	s2=++s1;
	cout<<"s1: "; s1.show();
	cout<<"s2: "; s2.show();
	if(s1==s2) cout<<"s1=s2"<<endl;
	else cout<<"s1!=s2"<<endl;
	return 0;
}
```
![alt text](image-46.png)

```c++
/*
请将如下程序补充完整，使得程序运行时的输出结果为：
Base::display
Derive::display const
Derive::display
Derive::display const

注意：仅允许在指定的下划线处填写内容，并删除下划线及其编号，不允许增加或删除语句，也不得改动程序中的其他部分。
试题源程序如下：
*/
#include <iostream>
using namespace std;
class Base {
	public:

/**********FILL**********/
		    virtual void display()   const   {
			cout<<"Base::display"<< endl;
		}
};
class Derive:public Base {
	public:

/**********FILL**********/
		void display() const     ;
		void display() { cout<<"Derive::display"<<endl; }
};
void Derive::display() const {
	cout<<"Derive::display const"<<endl;
}
void Display(Base *p) {

/**********FILL**********/
	    p->display();
}
int main() {
	Base b1; Derive d1;

/**********FILL**********/
	Derive const d2     ;
	Display(&b1); Display(&d1);
	d1.display(); d2.display();
	return 0;
}
```

```c++
/*
给定的程序在时钟（Clock）类定义的基础上，重载前置"++"运算符，实现时钟秒数（second）加1的操作；重载"<<"运算符，用于输出时钟对象，输出格式为"时:分:秒"。
请改正程序中指定位置的错误，使得程序运行时输出正确结果。
程序运行时，若输入：
23 59 59
则输出结果为：
时钟c1：23:59:59
时钟c2：0:0:0
时钟c1：0:0:0
时钟c2：0:0:0

注意：只允许修改注释"ERROR"的下一行，不得改动程序中的其他内容，也不允许增加或删减语句。
试题源程序：
*/
#include<iostream>
using namespace std;
class Clock	{
	public:
		Clock(int h=0,int m=0,int s=0) { //构造函数，h, m, s分别用于初始化时钟的时，分，秒
			this->hour=h; this->minute=m; this->second=s;
		}
		Clock& operator ++();

/**********ERROR**********/
		friend ostream& operator <<(ostream &out, const Clock &c);
	private:
		int hour,minute,second; // 时钟的时，分，秒（24小时制）
};

/**********ERROR**********/
Clock & Clock::operator ++() {
	second++;
	if (second>=60) {
		second-=60;
		minute++;
		if (minute>=60) {
			minute-=60;
			hour=(hour+1)%24;
		}
	}

/**********ERROR**********/
	return *this;
}
ostream & operator <<(ostream &out, const Clock &c) {
	out << c.hour << ":" << c.minute << ":" << c.second;

/**********ERROR**********/
	return out;
}
int main() {
	int h,m,s;
	cin>>h>>m>>s;
	Clock c1(h,m,s),c2;
	cout<<"时钟c1："<<c1<<endl;
	cout<<"时钟c2："<<c2<<endl;
	c2=++c1;
	cout<<"时钟c1："<<c1<<endl;
	cout<<"时钟c2："<<c2<<endl;
 	return 0;
 }
```
输入输出的重载作为友元函数（可以成员函数，但是调用写法为p1<<std::cout）

```c++
/*
点（Point）类的成员说明如下：
class Point {
	public:
		Point(int xx=0,int yy=0) { x=xx; y=yy;}
		void display() const;  // 输出点的位置，输出格式为“(x, y)”
	private:
		int x, y;  // x, y分别表示点的横坐标、纵坐标
};
请在Point类定义基础上，完成以下运算符重载：
（1）以成员函数形式重载后置“++”运算，使横坐标和纵坐标分别加1。
（2）将运算符“==”重载为非成员函数，用于判断两个点对象是否相等（对应坐标相等），若相等则返回true，否则返回false。
（3）重载“>>”运算符，用于点对象的输入（输入格式为“x y”）。
程序运行时，若依次输入：
0 0
1 1
则依次输出：
点p1: (0, 0)
点p2: (1, 1)
p1与p2不相等！
点p1: (1, 1)
点p3: (0, 0)
p1与p3不相等！

注意：部分源程序已给出，仅允许在注释“Begin”和“End”之间填写内容，不得改动其他已有的任何内容。
试题程序: 
*/
#include<iostream>
#include<fstream>
using namespace std;

 /*********Begin*********/
class Point {
	public:
		Point(int xx=0,int yy=0) { x=xx; y=yy;}
		void display() const;  // 输出点的位置，输出格式为“(x, y)”
		Point operator ++(int nouse);
		friend bool operator == (const Point & p,const Point& p2) ;
		friend istream& operator >> (istream& in ,Point& p);
	private:
		int x, y;  // x, y分别表示点的横坐标、纵坐标
};

Point Point::operator ++(int nouse)
{
	Point t = *this;
	this->x++,this->y++;
	return t;
}

bool operator == (const Point & p,const Point& p2) 
{
	return (p2.x == p.x)&&(p2.y == p.y);
}

istream& operator >> (istream& in ,Point& p)
{
	in>>p.x>>p.y;
	return in;
}

 /**********End**********/

void Point::display() const {  // 输出点的位置
	cout << "(" << this->x << ", " << this->y << ")" << endl;
}
int main() {
	Point p1,p2,p3;
	cin>>p1>>p2;
	cout<<endl;
	cout<<"点p1: "; p1.display();
	cout<<"点p2: "; p2.display();
	if(p1==p2) cout<<"p1与p2相等！"<<endl;
	else cout<<"p1与p2不相等！"<<endl;
	p3=p1++;
	cout<<"点p1: "; p1.display();
	cout<<"点p3: "; p3.display();
	if(p1==p3) cout<<"p1与p3相等！"<<endl;
	else cout<<"p1与p3不相等！"<<endl;	
	ifstream in1("8.2.1.2_3-s2.in");
	ofstream out1("8.2.1.2_3-s2.out");
	streambuf *cinbackup;
	streambuf *coutbackup;
	cinbackup=cin.rdbuf(in1.rdbuf());
	coutbackup=cout.rdbuf(out1.rdbuf());
	while(in1>>p1>>p2) {
		cout<<"点p1: "; p1.display();
		cout<<"点p2: "; p2.display();
		if(p1==p2) cout<<"p1与p2相等！"<<endl;
		else cout<<"p1与p2不相等！"<<endl;
		p3=p1++;
		cout<<"点p1: "; p1.display();
		cout<<"点p3: "; p3.display();
		if(p1==p3) cout<<"p1与p3相等！"<<endl<<endl;
		else cout<<"p1与p3不相等！"<<endl<<endl;
	}
	cin.rdbuf(cinbackup);
	cout.rdbuf(coutbackup);
	in1.close();
	out1.close();
	return 0;
}
```

重载++的详细见week test1